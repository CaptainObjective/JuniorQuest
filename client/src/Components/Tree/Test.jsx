import React from 'react';
import QuestTree from './QuestTree';

const TreeTest = props => {
  return (
    <React.Fragment>
      <QuestTree />
    </React.Fragment>
  );
};

export default TreeTest;
